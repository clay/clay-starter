<!-- data-uri="{{{ default _ref _self }}}" -->
{{#if @root.locals.edit}}
  <meta property="article:tag" content="{{ join (map tags (getProp 'text')) }}">
  <meta property="sailthru.tags" content="{{ join (map tags (getProp 'text')) }}">
{{else}}
  <meta property="article:tag" content="{{ join tags }}">
  <meta property="sailthru.tags" content="{{ join tags }}">
{{/if}}
